"use client"

// This component is now empty - auth functionality is disabled
// but we'll keep the file around for future reference
export function AuthButtons() {
  // Return empty fragment so nothing is rendered
  return <></>;
}
